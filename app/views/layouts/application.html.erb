<!DOCTYPE html>
<html>
  <head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>

    
    <title>TheGossipProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

          <nav class="navbar navbar-expand-lg navbar-light " style="background-color: #e3f2fd">
            <%if( session[:user_name] ) then nm= session[:user_name] else nm=" " end %>
            <%=link_to "The gossip project", welcome_path(name:nm),class:"navbar-brand"%>
        <button class="navbar-toggler" 
        data-toggle="collapse" data-target = "#navbaritem" >
          
          <a class="navbar-toggler-icon"></a>
        </button>

        <div class="collapse navbar-collapse" id="navbaritem">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">

              <%=link_to "Home", welcome_path(name:nm),class:"nav-link"%>
            </li>
              <li class="nav-item">
                <%= link_to "Contact",contact_contactView_path,class:"nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Team",team_teamView_path,class:"nav-link" %>
              </li>              
                  <div class="btn-group">
                    <button type="button" class="btn btn-infos dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                      <%if( session[:user_id] ) then str="Profile" else str="Login/Sign-up"  end %>
                      <%=str%>
                    </button>
                    <div class="dropdown-menu">
                      <%if( session[:user_id] ) then %>
                      <%= link_to "Se deconnecter", session_path(id:session[:user_id]) ,method:"delete",class:"dropdown-item"%>
                      <%= link_to "Voir profil", author_path(id:session[:user_id]),class:"dropdown-item"%>
                      <%else%>
                      <%= link_to "Se connecter", new_session_path ,class:"dropdown-item"%>
                      <%= link_to "S'inscrire", new_author_path ,class:"dropdown-item"%>
                      <%end%>                      
                    </div>
                  </div>
              </li>
          </ul>

        </div>

      </nav>

  </head>

  <body>
    <%= yield %>
  </body>
</html>
