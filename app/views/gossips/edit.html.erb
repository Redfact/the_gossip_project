<!DOCTYPE html>
<html>
<head>
	<title>MModifier gossip </title>
</head>
	<body>
	    <div class="row justify-content-center">
		<%= form_for(@gossip) do |f| %>

		  <div class="field">
		    <%= f.label :title %><br>
		    <%= f.text_field :title %>
		  </div><br><br>


			<div class="form-group" >
				<div class="field" style=>
			    <%= f.label :content %><br>
			    <%= f.text_area :content, :rows=>10, :cols=>60 %>
    			
			 	</div>
		  	</div>

		  <div class="actions">
		    <%= f.submit %>
		  </div>
			<% end %>		
	    </div>

	    <% if @gossip.errors.any? %>
	    <div class="row justify-content-center "style="padding-top: 1cm;">
			<div class="card card text-white bg-danger mb-3" style="width: 30cm;">
			<div class="card-header text-uppercase font-weight-bold">
			Attention erreur de saisis !
			</div>
			<div class="card-body">
			  <p>Nous n'avons pas réussi à modifier le potin pour la (ou les) raison(s) suivante(s) :</p>
			  <ul>
			    <% @gossip.errors.full_messages.each do |message| %>
			      <li><%= message %></li>
			    <% end %>
			  </ul>
			</div>
			</div>
		</div>
		<% end %>

	</body>
</html>